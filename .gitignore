node_modules/
dist/
*.log
.DS_Store
.vite/

# Rust / WASM build artifacts (CI rebuilds these)
bench-engine/target/
bench-engine/pkg/

# Claude Code harness state (lock files only — settings/agents stay trackable)
.claude/*.lock
